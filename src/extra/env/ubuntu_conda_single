echo "Loading basic ubuntu-conda environment (single precision)"

# this defaults to ia64, but we will use gfortran, not ifort
export GFDL_MKMF_TEMPLATE=ubuntu_conda_single

export F90=mpifort
export CC=mpicc
